[Unit]
Description=Fun Voice Ryan on-demand %i ASR worker

[Service]
Type=simple
# This template is started only by the daemon after recording completes. It has
# no [Install] section, so it cannot become a login-time model service.
ExecStart=%h/.local/bin/fun-voice-worker --profile %i
Restart=no
TimeoutStopSec=30
